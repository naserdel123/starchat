services:
  - type: static
    name: starchat-web
    buildCommand: cd frontend && flutter build web --release
    publishDir: frontend/build/web
    envVars:
      - key: FLUTTER_VERSION
        value: 3.16.0
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
